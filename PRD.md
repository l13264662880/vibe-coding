# PRD｜强制排序待办小工具

> 项目：vibe-coding ｜ 阶段：第 1 周 · Day 4 ｜ 版本：v1.1（Day 4 定稿；同日 AI 自检修复：补「撤销删除」功能与空列表边界文案）
> 上游：`research.md`（Day 3 需求研究）｜ 下游：技术选型（Day 5）、MVP 开发（Day 7）
> 修改规则：改了范围必须同步改第 5 节验收标准，两边永远一致。

## 1｜产品概述

一个极简网页待办清单：所有任务排在**一条唯一的队列**里，位置就是优先级，页面永远突出显示「第一件该做的事」。

- **给谁用**：我自己 + 身边的人，单人使用。
- **和竞品的本质区别**：滴答清单、Todoist、微软待办的优先级都是「贴标签」（允许 8 件事并列"高优先级"）；本产品是「强制排序」——第 1 名只有一个，并列不存在。
- **解决的问题**：事情都记下来了，但没人说得清"现在先干哪件"（详见 research.md 第 1 节）。

## 2｜用户与场景

沿用 research.md 第 2 节的结论，不重新发明：

- **场景 1｜期末周**：8 门课要复习，说不清先复习哪门。
- **场景 2｜搬家打包**：一屋子东西，不知道先装哪箱。
- **场景 3｜找工作投递**：几家公司，投递批次排不出先后。

共同点：事情都在脑子里，缺的不是"再记一遍"，而是"先干哪件"的唯一答案。

## 3｜做什么（MVP 功能清单，Day 7 交付范围）

1. **添加任务**：输入一句话，回车后任务出现在列表末尾。
2. **拖拽排序**：按住任意任务拖到新位置，松手立即生效；位置即优先级。
3. **第一件事高亮**：页面固定突出显示当前第 1 位的未完成任务，同一时刻有且只有一个；列表为空或没有未完成任务时，该区域显示「先加一件事」引导文案，不空白、不报错。
4. **标记完成**：任务可标记完成——划线显示、自动沉到列表底部，不再参与排序和「第一件事」。完成的任务不消失、不单独成列表（否则变相分类）。
5. **删除任务**：每条任务可删除，删除后其余任务自动补位、相对顺序不变。
6. **撤销删除**：删除任务后页面出现「撤销」入口，点击后该任务回到删除前的位置；进行新操作或刷新页面后撤销失效。
7. **编辑任务**：可修改任务文字，保存后位置不变。
8. **数据保存**：存浏览器 localStorage，刷新、关闭浏览器再打开，任务和顺序不丢。

## 4｜不做什么（砍掉的功能及理由）

### 4.1 范围外（Day 3 研究已定，沿用 research.md 第 6 节）

| 不做 | 为什么砍 |
| --- | --- |
| 账号注册 / 登录 | 单人工具，数据存浏览器本地；账号系统需要后端，超出 Day 7 一个人几天的承载力 |
| 多人协作 / 共享清单 | 单人使用是产品前提，协作会改变产品本质 |
| 手机 App / 小程序 | 网页在手机浏览器能打开即可，原生开发是另一条产品线 |
| 日历视图 / 提醒推送 | 本产品只回答「先做哪件」，「什么时候做」是另一个产品的事 |
| 四象限视图 | Day 3 研究的核心结论：分类不是要解决的问题，这是本项目立项的转折点 |
| 数据导入导出 | localStorage 对单人本地使用绰绰有余，导入导出服务的是"要搬家的人"，现在没有这个用户 |
| AI 建议排序 | 排序的价值在于用户自己比较的过程，AI 代排等于抢走产品核心体验 |
| 数据库 | Day 23 才需要后端存储，届时用 `.env` 配置，MVP 阶段不接 |

### 4.2 功能内取舍（Day 4 新砍）

| 功能 | 决定 | 理由 |
| --- | --- | --- |
| 多清单（学习 / 生活分列表） | **砍** | 多清单 = 变相分类，直接违背「位置唯一」哲学。所有事进同一个列表，用排序本身分轻重。代价：所有事情挤在一个列表——这正是产品要的效果。 |

本轮一并审过、**明确留下**的：删除（输错和做完的事要能清走，否则稀释「第一件事」）、编辑（打错字能改，删了重输太糙）、撤销（有了删除和编辑，误操作找回值得）、标记完成（事情做完要有归宿，否则列表变垃圾场）、拖拽（research.md 原方案，体验最直觉）。

## 5｜验收标准（Day 7 交付时逐条打勾）

> Day 7 实现顺序建议：先做核心闭环（1-4、9），再做体验补全（5-8、10）；做不完按此顺序取舍——宁可把后面的砍进第 6 节排队，不硬撑。

- [ ] 1. 打开页面，输入文字按回车，任务出现在列表末尾。
- [ ] 2. 拖拽任意任务到新位置，松手后顺序立即更新。
- [ ] 3. 同一时刻每个位置只有一条任务，「第 1 位」全局唯一，不存在并列第一。
- [ ] 4. 页面上有且只有一个「第一件事」的突出显示，且永远指向当前第 1 位的未完成任务；列表为空或没有未完成任务时，该区域显示「先加一件事」引导文案，不空白、不报错。
- [ ] 5. 删除任意任务后，其余任务自动补位，相对顺序不变。
- [ ] 6. 删除任意任务后点击「撤销」，该任务回到删除前的位置；刷新页面后撤销入口不再出现。
- [ ] 7. 编辑任意任务的文字，保存后它的位置不变。
- [ ] 8. 标记完成后：任务划线显示、自动沉到列表底部，不再出现在「第一件事」。
- [ ] 9. 刷新或关闭浏览器再打开，任务内容和顺序都还在（localStorage）。
- [ ] 10. 页面上找不到第 4.1 节表格和 4.2 表格所列任何功能的入口（范围自检：MVP 没做多的）。

每条都是"能亲眼看到、亲手点到"的行为，不写"体验流畅"这类无法验收的形容词。

## 6｜后续迭代（排队区，不承诺时间）

| 功能 | 排队理由 / 唤醒条件 |
| --- | --- |
| 数据导入导出 | 换设备或清浏览器数据时需要；Day 23 数据库到位后优先级上升 |
| 提醒 / 通知 | 先解决「先做哪件」，再解决「别忘了做」；依赖账号和推送能力 |
| 账号 / 云同步 | Day 23 接数据库后再议，当前 localStorage 够用 |
| 预排序（工具给初始顺序，用户可改） | **Day 7 记录**（原条目名「AI 建议排序」）：工具只给起点，最终顺序仍由用户拖拽决定——区别于第 4.1 节砍掉的「AI 代排」。用户进一步明确：起点要**根据用户身份信息、有依据地**排，不能随机。⚠️ 该依据依赖「用户身份」数据，而 4.1 已砍账号 —— 前置问题详见 6.1 |
| 四象限视图 | 大概率永远不做——Day 3 研究结论：分类不是要解决的问题 |
| 多清单 | 大概率永远不做——与「位置唯一」哲学冲突，除非出现单人也需要分域的真实场景 |
| 手机 App | 网页版被高频使用后再考虑 |

### 6.1 「预排序」补充说明（Day 7 记录）

**需求**：工具给一个初始顺序当起点，用户仍可拖拽修改——不是替用户拍板。
**用户额外要求（Day 7 明确）**：起点不能随机，要**根据用户身份信息、有依据地**排序。

**必须先回答的前置问题**：本产品在第 4.1 节已砍掉「账号注册 / 登录」（单人本地工具，打开即用）。**没有账号，工具就拿不到「用户身份信息」。** 因此这条功能的前置不是排序算法，而是先定「身份从哪来」：

| 身份从哪来 | 代价 |
| --- | --- |
| 首次使用时用户自己填（存本地，不涉及账号） | 多一步上手成本，与「上手零门槛」有摩擦 |
| 绕开身份，改用任务自身的元信息（截止日期 / 重要度） | 要给任务加字段、界面变复杂；但不需要账号 |
| 真接账号，拿到真实用户身份 | 推翻第 4.1 节，依赖后端与登录能力，排到 Day 23 之后再议 |

**唤醒条件**：先确认手动「强制排序」真的被高频使用（第一版上线后观察）。动手前须定清「身份从哪来」与「起点由什么决定」，并**同步改第 5 节验收标准**。

---

*本 PRD 的每一条砍掉项都有理由，每一条验收标准都可打勾。Day 5 技术选型、Day 7 开发只对第 3 节负责。*
