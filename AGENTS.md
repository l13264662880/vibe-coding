# AGENTS.md — 项目规则文件（vibe coding 项目）

> 这个文件是 AI 助手每次动手前的“开机手册”。AI 开始干活之前，必须先读它，再按里面的规则行动。

## 项目信息
- 项目名：vibe-coding
- 当前阶段：第 1 周 · Day 6（检查并完善项目规则文件 AGENTS.md）
- 技术栈：纯前端单页 —— HTML + CSS + 原生 JS + 浏览器 localStorage（Day 5 定稿）
- 负责人：学员本人

## 给 AI 的硬规则（每次动手前必读）
1. **先读本文件**：动手前必须阅读 AGENTS.md，并按其中规则执行。
2. **先确认再动手**：写代码或改文件前，先说清楚要做什么、改哪里、为什么。
3. **不可逆操作先征求同意**：删除、覆盖、推送、发布等操作，必须先获得用户确认。
4. **事实可追溯**：每个数字、结论都要有来源或可复现的计算，不编造、不猜测。
5. **敏感信息不落盘**：密码、密钥、token 等绝不写入代码或文件。
6. **交付前自查**：交付前必须验证结果可用（跑通命令、检查产物、说明验证方式）。
7. **当天只做当天任务**：只做学员当天清单里列出的事；学员没有明确说「进入下一板块」，不得擅自往下推进，也不得提前做后面天数的任务。清单外想到的改进，先说一句，等学员点头。（Day 6 追加）
8. **每次改动附验证证据**：每次改动都要给出学员能亲眼确认的证据（文件内容、命令输出、页面效果），不接受口头「已完成」。（Day 6 追加）
9. **积分紧张时优先短答**：跳过寒暄和客套，直接给结果；不铺垫、不复述学员已知的信息。（Day 6 追加）
10. **文档冲突时必须停下来问**：发现两份文档或两处内容互相矛盾时，先停下说明冲突在哪、再提问，不自行挑一方继续往下做。（Day 6 追加）
11. **回答和注释用中文**：与学员的对话、文档正文、代码注释，一律使用简体中文。（Day 6 追加）
12. **有明显争议时用表格给选项**：遇到需要学员拍板、且存在明显分歧的情况，列成可直接选择的表格（每个选项写清含义和代价）发给学员，不用长段落描述。（Day 6 追加）

## 常用命令（环境自检）
- `git --version`   # 查看 Git 版本
- `node --version`  # 查看 Node.js 版本
- `npm --version`   # 查看 npm 版本

## 阶段约定
- Day 1：装环境、注册账号、建工作区、存本规则文件。（已完成）
- Day 2：建仓库、开始写第一行代码。（已完成：仓库已建、index.html 占位页已提交）
- Day 3：需求研究，产出 research.md（项目方向：强制排序待办小工具）。（已完成）
- Day 4：写 PRD，产出 PRD.md（8 项功能 + 10 条验收标准）。（已完成）
- Day 5：技术方案，产出 TECH_DESIGN.md + data-flow.svg（技术路线：纯前端单页）。（已完成，于 Day 6 当天补做）
- Day 6：检查项目规则文件 AGENTS.md，追加 6 条自己的硬规则。（已完成）
- Day 7：开发网站 MVP（替换占位页，技术方案见 TECH_DESIGN.md）。
- Day 23：接数据库，届时才用上 `.env`。
